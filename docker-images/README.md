Build the image using following commands

docker build . -t flask-image-two:v1

docker image

docker tag <image_id> <dockerhub_username>/flask-image-one:v1

docker push <dockerhub_username>/flask-image-one:v1


